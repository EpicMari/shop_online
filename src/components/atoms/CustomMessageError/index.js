import React from "react";
import { StyledDiv } from "./StyledCustomMessageError";

const CustomMessageError = ({ children }) => {
  return <StyledDiv>{children}</StyledDiv>;
};

export default CustomMessageError;
